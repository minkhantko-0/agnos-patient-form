# Supabase project used for Realtime channels only.
# No tables, no migrations, no auth — see README "Real-time synchronisation flow".
# Both values: Supabase dashboard → Project Settings → API Keys
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
